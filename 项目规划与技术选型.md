# 个人知识库（Memora）项目规划与技术选型

## Context

构建一个开源的个人知识库应用，核心解决 LLM 用户的三个痛点：
1. **对话记忆散落**：每次新对话从零开始，之前的经验和决策丢失
2. **Prompt 经验难复用**：好用的 prompt 散落在各处，无法版本管理和优化
3. **知识碎片化**：文档、代码、网页、对话中的知识无法统一检索

**核心差异化**：MCP 自动记忆提取——通过 MCP 协议让 Claude Code 等 AI 工具在对话过程中自动提取有价值的信息并存入知识库，实现**零成本知识积累**。这是项目能否成功的关键能力。

**产品形态**：独立 CLI 应用 + Web 可视化 + MCP 接口（供 Claude Code 等工具调用）
**目标用户**：LLM 开发者（CLI 优先） + 知识工作者（Web 可视化）
**存储策略**：本地优先，支持数据导出，为未来云端迁移预留接口

---

## 一、架构总览

```
┌──────────────────────────────────────────────────────────┐
│                      展示层                               │
│  ┌──────────┐  ┌──────────────┐  ┌────────────────────┐  │
│  │  CLI      │  │  Web Frontend│  │  MCP Server        │  │
│  │  (Typer)  │  │  (Vue 3)     │  │  (Claude Code等)   │  │
│  └─────┬─────┘  └──────┬───────┘  └────────┬───────────┘  │
│        │               │                   │              │
│        └───────────────┼───────────────────┘              │
│                        ▼                                  │
│              ┌──────────────────┐                         │
│              │   API 层 (FastAPI)│                         │
│              │   REST + SSE      │                         │
│              └────────┬─────────┘                         │
├───────────────────────┼───────────────────────────────────┤
│                   核心层 (Core)                             │
│                       ▼                                   │
│  ┌─────────────────────────────────────────────────────┐  │
│  │                    三大引擎                           │  │
│  │  ┌──────────┐  ┌──────────────┐  ┌──────────────┐  │  │
│  │  │记忆引擎   │  │RAG 引擎      │  │Prompt 引擎   │  │  │
│  │  │          │  │              │  │              │  │  │
│  │  │-对话摘要  │  │-文档摄入     │  │-版本管理     │  │  │
│  │  │-事实提取  │  │-分块/嵌入    │  │-评分/A/B测试 │  │  │
│  │  │-偏好记忆  │  │-语义检索     │  │-优化建议     │  │  │
│  │  │-跨会话检索│  │-多源接入     │  │-模板库       │  │  │
│  │  └──────────┘  └──────────────┘  └──────────────┘  │  │
│  └─────────────────────────────────────────────────────┘  │
├───────────────────────────────────────────────────────────┤
│                    存储层 (Storage)                         │
│  ┌──────────┐  ┌──────────────┐  ┌────────────────────┐  │
│  │ SQLite    │  │ ChromaDB     │  │ 文件系统            │  │
│  │ 结构化数据│  │ 向量存储      │  │ 原始文档            │  │
│  │(记忆/Prompt│  │(语义检索)    │  │(Markdown/PDF等)    │  │
│  │ /元数据)  │  │              │  │                    │  │
│  └──────────┘  └──────────────┘  └────────────────────┘  │
└───────────────────────────────────────────────────────────┘
```

---

## 二、项目目录结构

```
memora/
├── pyproject.toml
├── .env.example
├── README.md
│
├── config/
│   ├── prompts/                      # Agent Prompt 模板
│   │   ├── memory_extract.yaml       # 记忆提取 prompt
│   │   ├── memory_summarize.yaml     # 对话摘要 prompt
│   │   ├── rag_query.yaml            # RAG 查询 prompt
│   │   └── prompt_optimize.yaml      # Prompt 优化 prompt
│   └── settings.yaml                 # 全局配置
│
├── src/memora/
│   ├── __init__.py
│   │
│   ├── agent/                        # LangChain Agent
│   │   ├── __init__.py
│   │   ├── graph.py                  # 主图定义
│   │   ├── state.py                  # 共享状态
│   │   └── nodes.py                  # 图节点
│   │
│   ├── memory/                       # 记忆引擎
│   │   ├── __init__.py
│   │   ├── extractor.py              # 从对话中提取 facts/偏好/决策
│   │   ├── store.py                  # 记忆存储与检索
│   │   ├── summarizer.py             # 对话摘要生成
│   │   └── models.py                 # 记忆数据模型
│   │
│   ├── rag/                          # RAG 引擎
│   │   ├── __init__.py
│   │   ├── ingester.py               # 文档摄入（加载 + 分块）
│   │   ├── embedder.py               # 嵌入生成
│   │   ├── retriever.py              # 语义检索
│   │   ├── loaders/                  # 多格式加载器
│   │   │   ├── __init__.py
│   │   │   ├── markdown.py
│   │   │   ├── pdf.py
│   │   │   ├── code.py               # 代码文件加载
│   │   │   ├── webpage.py            # 网页内容抓取
│   │   │   └── conversation.py       # 对话记录导入
│   │   └── chunker.py                # 文本分块策略
│   │
│   ├── prompt/                       # Prompt 引擎
│   │   ├── __init__.py
│   │   ├── manager.py                # Prompt CRUD + 版本管理
│   │   ├── optimizer.py              # Prompt 优化建议
│   │   ├── tester.py                 # Prompt A/B 测试
│   │   └── models.py                 # Prompt 数据模型
│   │
│   ├── mcp/                          # MCP Server
│   │   ├── __init__.py
│   │   ├── server.py                 # MCP 服务入口
│   │   └── tools.py                  # MCP 工具定义
│   │
│   ├── storage/                      # 存储层
│   │   ├── __init__.py
│   │   ├── database.py               # SQLite 连接管理
│   │   ├── vector_store.py           # ChromaDB 封装
│   │   ├── migrations.py             # 数据库迁移
│   │   └── exporter.py               # 数据导出（JSON/CSV）
│   │
│   ├── models/                       # Pydantic 数据模型
│   │   ├── __init__.py
│   │   ├── memory.py                 # 记忆模型
│   │   ├── document.py               # 文档模型
│   │   ├── prompt.py                 # Prompt 模型
│   │   └── search.py                 # 搜索结果模型
│   │
│   ├── cli/                          # CLI 入口
│   │   ├── __init__.py
│   │   └── main.py                   # Typer CLI
│   │
│   ├── api/                          # FastAPI Web API
│   │   ├── __init__.py
│   │   ├── app.py
│   │   └── routes/
│   │       ├── __init__.py
│   │       ├── memory.py             # 记忆相关接口
│   │       ├── knowledge.py          # 知识库接口
│   │       ├── prompt.py             # Prompt 管理接口
│   │       └── search.py             # 统一搜索接口
│   │
│   └── utils/
│       ├── __init__.py
│       ├── text.py                   # 文本处理工具
│       └── config.py                 # 配置加载
│
├── web/                              # Vue 3 前端
│   ├── package.json
│   └── src/
│       ├── App.vue
│       ├── views/
│       │   ├── DashboardView.vue     # 仪表盘（知识概览）
│       │   ├── MemoryView.vue        # 记忆浏览与搜索
│       │   ├── KnowledgeView.vue     # 知识库管理
│       │   └── PromptView.vue        # Prompt 管理
│       └── components/
│           ├── SearchBar.vue         # 统一搜索栏
│           ├── KnowledgeGraph.vue    # 知识图谱可视化
│           ├── MemoryTimeline.vue    # 记忆时间线
│           ├── PromptEditor.vue      # Prompt 编辑器
│           └── StatsPanel.vue        # 统计面板
│
├── data/                             # 本地数据目录
│   ├── memora.db                     # SQLite 数据库
│   ├── chroma/                       # ChromaDB 向量数据
│   └── documents/                    # 原始文档存储
│
└── tests/
    ├── conftest.py
    ├── test_memory.py
    ├── test_rag.py
    ├── test_prompt.py
    └── test_api.py
```

---

## 三、技术选型

### 3.1 核心依赖

| 层次 | 选型 | 理由 |
|---|---|---|
| **LLM** | DashScope (Qwen) via OpenAI 兼容接口 | 中文能力强，国内访问快 |
| **Embedding** | DashScope text-embedding-v3 | 与 LLM 同一平台，中文支持好 |
| **LLM 框架** | **LangChain**（完整包） | LLM 抽象、结构化输出、文本分块；完整包为后续扩展预留 |
| **向量数据库** | **ChromaDB** | 纯 Python、本地运行、无需外部服务、API 简洁 |
| **关系数据库** | SQLite + aiosqlite | 零配置、单文件、支持导出、本地优先 |
| **Web API** | FastAPI | 异步原生，SSE 支持好 |
| **CLI** | Typer + Rich | |
| **HTTP 客户端** | httpx | 异步调用外部 API |
| **文档解析** | beautifulsoup4（网页）、pypdf2（PDF） | |
| **文本分块** | langchain_text_splitters | 成熟可靠，支持 Markdown/代码/递归等多种策略 |
| **配置** | python-dotenv + PyYAML | |

### 3.2 依赖关系

```
直接依赖：
  langchain              → 完整包，为后续功能扩展预留（Chain、Memory、Agent 等）
  langchain-openai       → 连接 DashScope（OpenAI 兼容接口）
  langchain-text-splitters → 文本分块
  chromadb               → 向量存储
  aiosqlite              → 异步 SQLite
  fastapi + uvicorn      → Web API
  typer + rich           → CLI
  httpx                  → HTTP 客户端
  pydantic               → 数据模型
  pyyaml                 → 配置
  beautifulsoup4         → 网页解析
  pypdf2                 → PDF 解析

MVP 阶段实际使用的 LangChain 能力：
  - ChatOpenAI + with_structured_output → LLM 结构化输出（记忆提取）
  - TextSplitter 系列 → 文档分块
  - 后续可按需启用 Chain、Agent、Memory 等模块
```

### 3.3 存储设计

```
SQLite 表结构：
  memories          → 记忆条目（fact、preference、decision）
  conversations     → 对话记录摘要
  prompts           → Prompt 元数据
  prompt_versions   → Prompt 版本历史
  documents         → 文档元数据（路径、类型、摄入时间）
  tags              → 标签
  memory_tags       → 记忆-标签关联
  search_history    → 搜索历史

ChromaDB 集合：
  memories          → 记忆的向量嵌入（语义检索）
  documents         → 文档分块的向量嵌入（RAG 检索）
  prompts           → Prompt 的向量嵌入（相似 Prompt 查找）
```

---

## 四、核心模块设计

### 4.1 记忆引擎

**核心能力**：从对话中自动提取有价值的信息，跨会话检索。

```python
# 记忆类型
class MemoryType(str, Enum):
    FACT = "fact"              # 事实："用户是后端开发，用 Go 为主"
    PREFERENCE = "preference"  # 偏好："用户不喜欢 mock 测试"
    DECISION = "decision"      # 决策："项目选了 Vue 3 而非 React"
    EXPERIENCE = "experience"  # 经验："上次部署踩了 XX 的坑"
    RELATIONSHIP = "relationship" # 关系："张三负责前端模块"

# 记忆提取流程
对话消息 → LLM 提取（结构化输出）→ 去重/合并 → 存入 SQLite + 向量化 → ChromaDB

# 记忆检索流程
用户查询 → 向量相似度搜索 + 关键词过滤 → 排序 → 返回 Top-K 记忆
```

**提取策略**：
- 每轮对话结束后，异步提取新记忆
- 与已有记忆做去重和合并（相似度 > 0.9 视为重复）
- 支持手动标记重要记忆 / 删除错误记忆

### 4.2 RAG 引擎

**核心能力**：多格式文档摄入，语义检索。

```python
# 文档摄入流程
原始文件 → Loader 加载 → 文本分块 → Embedding → ChromaDB

# 支持的格式
.md    → 直接读取，按标题分块
.pdf   → pypdf2 提取文本，按段落分块
.py/.js/.ts → tree-sitter 解析 AST，按函数/类分块
URL    → beautifulsoup4 提取正文，按段落分块
对话记录 → JSON 解析，按轮次分块

# 检索流程
用户查询 → Embedding → ChromaDB 向量搜索 → Reranker（可选）→ 返回 Top-K 分块
```

**分块策略**：
- Markdown：按 `#` 标题层级分割，保持章节完整性
- 代码：按函数/类分割，保留上下文（函数签名 + docstring）
- 通用：滑动窗口 + 重叠，chunk_size=512, overlap=64

### 4.3 Prompt 引擎

**核心能力**：Prompt 版本管理、评分、优化建议。

```python
# Prompt 数据结构
class Prompt(BaseModel):
    id: str
    name: str                    # "代码审查助手"
    description: str
    tags: list[str]
    created_at: datetime
    updated_at: datetime

class PromptVersion(BaseModel):
    id: str
    prompt_id: str
    version: int                 # 1, 2, 3...
    content: str                 # prompt 正文
    variables: list[str]         # 模板变量 {{input}}
    score: float | None          # 用户评分 1-5
    test_results: list[TestResult]  # A/B 测试结果
    created_at: datetime

# Prompt 管理操作
save(name, content, variables) → 创建新 Prompt
update(prompt_id, content)     → 创建新版本（不覆盖旧版本）
list(tag=...)                  → 列表查询
get(prompt_id, version=None)   → 获取指定版本（默认最新）
score(prompt_id, version, score) → 评分
compare(prompt_id, v1, v2)     → 对比两个版本
optimize(prompt_id, feedback)  → LLM 生成优化建议
```

### 4.4 统一搜索

**核心能力**：一个搜索框检索所有内容。

```python
# 搜索接口
async def unified_search(query: str, scope: str = "all", top_k: int = 10):
    """
    scope: "all" | "memory" | "knowledge" | "prompt"
    并行搜索三个引擎，合并排序返回
    """
    tasks = []
    if scope in ("all", "memory"):
        tasks.append(search_memory(query, top_k))
    if scope in ("all", "knowledge"):
        tasks.append(search_knowledge(query, top_k))
    if scope in ("all", "prompt"):
        tasks.append(search_prompts(query, top_k))

    results = await asyncio.gather(*tasks)
    return merge_and_rank(results)
```

### 4.5 MCP 自动记忆提取（核心差异化）

**这是项目能否成功的关键能力。** 核心理念：让用户"零成本"积累知识——AI 工具在对话过程中自动提取有价值的信息并存入 Memora，用户无需手动录入。

#### 工作原理

```
┌─────────────────────────────────────────────────────────────┐
│                     Claude Code / Cursor 等 AI 工具          │
│                                                             │
│  对话过程：                                                  │
│  用户："这个项目用 Vue 3，部署在阿里云 ECS，用 Docker Compose"  │
│  Claude："好的，我来配置..."                                  │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  对话结束后，AI 工具通过 MCP 自动执行（用户不可见）：    │   │
│  │                                                      │   │
│  │  1. save_memory("项目前端: Vue 3", type="decision")   │   │
│  │  2. save_memory("部署环境: 阿里云 ECS", type="fact")  │   │
│  │  3. save_memory("编排工具: Docker Compose", type="fact")│  │
│  └──────────────────────────────────────────────────────┘   │
│                                                             │
│  下次新对话：                                                │
│  用户："帮我加一个新页面"                                     │
│  Claude → MCP recall("前端相关") → 拿到之前的记忆             │
│  Claude："好的，基于你项目用的 Vue 3 + Pinia，我来..."         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 为什么这是破局点

```
手动录入知识库：
  第1天：兴致勃勃录入 20 条 → 第3天：忘了录入 → 第7天：放弃
  结果：数据断层，知识库变废墟 ❌

MCP 自动提取：
  每次对话自动提取 2-5 条 → 一个月积累 200+ 条 → 检索越来越准
  结果：飞轮转起来，越用越有价值 ✅

核心区别：把"录入"这个动作从用户身上完全拿掉
```

#### MCP 工具定义

```python
# === 记忆相关（自动调用） ===

@mcp_tool
async def save_memory(
    content: str,           # "项目前端技术栈: Vue 3 + Pinia"
    memory_type: str,       # "fact" | "preference" | "decision" | "experience"
    source: str = "claude_code"  # 来源标记
) -> Memory:
    """
    保存一条记忆到知识库。
    AI 工具在对话中识别到有价值信息时自动调用。
    内部流程：LLM 结构化提取 → 去重检查 → 存入 SQLite → 向量化 → ChromaDB
    """

@mcp_tool
async def recall(
    query: str,             # "前端技术栈" 或 "部署相关"
    top_k: int = 5,
    memory_type: str = None # 可选过滤类型
) -> list[Memory]:
    """
    从记忆中检索相关信息。
    AI 工具在新对话开始或需要上下文时自动调用。
    内部流程：query embedding → ChromaDB 向量搜索 → 排序 → 返回
    """

@mcp_tool
async def extract_and_save(
    messages: list[dict]    # 对话消息列表
) -> list[Memory]:
    """
    从一段对话中批量提取记忆（核心自动提取接口）。
    AI 工具在对话结束时调用，一次性提取所有有价值的信息。
    内部流程：
      1. LLM 分析对话，提取 facts/preferences/decisions
      2. 与已有记忆去重（相似度 > 0.9 跳过）
      3. 批量写入 SQLite + ChromaDB
      4. 返回本次提取的记忆列表
    """

# === 知识库相关 ===

@mcp_tool
async def search_knowledge(
    query: str,
    top_k: int = 5
) -> list[DocumentChunk]:
    """从知识库中检索相关文档片段（RAG 检索）"""

@mcp_tool
async def ingest_document(path_or_url: str) -> Document:
    """将文档摄入知识库（加载 → 分块 → 嵌入 → 存储）"""

# === Prompt 相关 ===

@mcp_tool
async def get_prompt(name: str) -> PromptVersion:
    """获取指定 Prompt 的最新版本"""

@mcp_tool
async def save_prompt(
    name: str,
    content: str,
    variables: list[str] = []
) -> Prompt:
    """保存一个 Prompt（首次创建或更新版本）"""

# === 查询相关 ===

@mcp_tool
async def list_memories(
    memory_type: str = None,
    limit: int = 20
) -> list[Memory]:
    """列出记忆条目"""

@mcp_tool
async def get_stats() -> dict:
    """获取知识库统计信息"""
```

#### Claude Code 集成配置

```json
// .claude/settings.json
{
  "mcpServers": {
    "memora": {
      "command": "memora",
      "args": ["mcp", "serve"],
      "description": "Memora 个人知识库 - 自动记忆提取与知识检索"
    }
  }
}
```

#### 自动提取的触发策略

```yaml
# config/settings.yaml 中可配置
memory_extraction:
  # 触发条件
  auto_extract: true                    # 是否开启自动提取
  min_messages: 3                       # 对话至少 3 轮才触发提取
  extract_on_end: true                  # 对话结束时自动提取

  # 提取策略
  extract_types:                        # 提取哪些类型
    - fact                              # 事实性信息
    - preference                        # 用户偏好
    - decision                          # 技术决策
    - experience                        # 经验教训

  # 去重策略
  dedup_threshold: 0.9                  # 相似度 > 0.9 视为重复
  merge_similar: true                   # 合并相似记忆而非跳过

  # 提取 Prompt（LLM 用于分析对话的 prompt）
  prompt_template: "memory_extract"
```

---

## 五、数据模型

```python
# models/memory.py
class Memory(BaseModel):
    id: str
    content: str                           # "用户偏好用 Go 写后端"
    memory_type: MemoryType                # fact/preference/decision/experience
    source: str                            # 来源："对话 #1234" 或 "手动添加"
    confidence: float                      # 置信度 0-1
    tags: list[str]
    created_at: datetime
    updated_at: datetime
    access_count: int                      # 被检索次数（用于衰减）

class ConversationSummary(BaseModel):
    id: str
    title: str
    summary: str
    key_points: list[str]
    memories_extracted: list[str]          # 关联的 memory id
    created_at: datetime

# models/document.py
class Document(BaseModel):
    id: str
    title: str
    source_path: str                       # 文件路径或 URL
    source_type: Literal["file", "url", "conversation"]
    file_type: str                         # "md", "pdf", "py", "webpage"
    chunk_count: int
    ingested_at: datetime
    metadata: dict                         # 额外元数据

class DocumentChunk(BaseModel):
    id: str
    document_id: str
    content: str
    chunk_index: int
    metadata: dict                         # {"heading": "## 安装", "page": 3}

# models/prompt.py
class Prompt(BaseModel):
    id: str
    name: str
    description: str
    tags: list[str]
    latest_version: int
    created_at: datetime

class PromptVersion(BaseModel):
    id: str
    prompt_id: str
    version: int
    content: str
    variables: list[str]                   # ["input", "context", "format"]
    score: float | None
    notes: str                             # 版本说明
    created_at: datetime

# models/search.py
class SearchResult(BaseModel):
    content: str
    source_type: Literal["memory", "document", "prompt"]
    source_id: str
    relevance_score: float
    highlight: str                         # 高亮片段
    metadata: dict
```

---

## 六、使用场景演示

### 6.1 MCP 自动记忆提取（在 Claude Code 中）

```bash
# 1. 配置 MCP Server（一次性）
# 编辑 .claude/settings.json，添加 memora MCP server

# 2. 正常使用 Claude Code，记忆自动积累

# 第一天：技术选型讨论
你："这个项目用 Vue 3 + Pinia，后端用 FastAPI"
Claude："好的，我来搭建项目骨架..."
# → Claude Code 自动调用 MCP save_memory × 3
#   save_memory("项目前端: Vue 3 + Pinia", type="decision")
#   save_memory("项目后端: FastAPI", type="decision")
#   save_memory("用户偏好 Vue 生态", type="preference")

# 第二天：部署相关讨论
你："部署到阿里云 ECS，用 Docker Compose"
Claude："我来写 Dockerfile 和 docker-compose.yml..."
# → 自动提取
#   save_memory("部署环境: 阿里云 ECS", type="fact")
#   save_memory("编排工具: Docker Compose", type="decision")

# 第三天：新对话，Claude Code 自动召回记忆
你："帮我加一个用户管理模块"
Claude（通过 MCP recall 拿到记忆）：
  "好的，基于你项目用的 Vue 3 + Pinia + FastAPI，我来设计用户管理模块..."
# → 无需重复说明技术栈，Claude 已经"记住"了
```

### 6.2 CLI 交互

```bash
$ memora

🧠 Memora 个人知识库 v0.1.0
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

命令：
  /ingest <路径/URL>    摄入文档到知识库
  /search <查询>        统一搜索（记忆+知识+Prompt）
  /memory               管理记忆（list/add/delete/export）
  /prompt               管理 Prompt（list/add/score/optimize）
  /chat                 对话模式（自动提取记忆）
  /stats                知识库统计
  /export               导出所有数据
  /web                  启动 Web 界面
  /help                 帮助
  /quit                 退出

> /ingest ./docs/api.md
📄 正在摄入: api.md
   分块: 23 个 | 嵌入: ✅ | 耗时: 3.2s
   ✅ 已添加到知识库

> /search 如何配置认证
🔍 搜索结果（3 条）：

  📝 [记忆] "上次讨论决定用 JWT 而非 session" (0.92)
  📄 [文档] api.md#认证模块 "Bearer token 认证流程..." (0.87)
  📄 [文档] auth.md "JWT 配置步骤..." (0.83)

> /prompt add "代码审查助手" --tags "code,review"
📝 输入 Prompt 内容（输入 END 结束）：
> 你是一个专业的代码审查专家...
> END
✅ 已保存 Prompt "代码审查助手" v1

> /stats
📊 知识库统计
  记忆: 156 条 (fact: 89, preference: 34, decision: 21, experience: 12)
  文档: 47 篇 (md: 30, pdf: 8, py: 6, webpage: 3)
  Prompt: 12 个 (avg score: 4.2)
  向量维度: 1024 | 总嵌入: 1,234 条
```

---

## 七、分阶段实施计划

### Phase 1：MVP（3-4 周）

**目标**：三大引擎最小可用 + **MCP 自动记忆提取跑通**，CLI + MCP 可用

> **核心验证点**：在 Claude Code 中配置 Memora MCP Server 后，对话结束自动提取记忆，新对话自动召回相关记忆。这个流程跑通 = 项目核心价值成立。

| 任务 | 优先级 | 预估工时 |
|---|---|---|
| 项目骨架（pyproject.toml、目录结构） | P0 | 2h |
| 数据模型定义（Pydantic） | P0 | 3h |
| 存储层：SQLite schema + migrations | P0 | 4h |
| 存储层：ChromaDB 封装 | P0 | 3h |
| 记忆引擎：事实提取（LLM 结构化输出） | P0 | 5h |
| 记忆引擎：存储与向量检索 | P0 | 4h |
| **MCP Server + 自动记忆提取** | **P0** | **6h** |
| **MCP 工具：extract_and_save（批量提取）** | **P0** | **3h** |
| **MCP 工具：recall（记忆召回）** | **P0** | **2h** |
| RAG 引擎：Markdown/文本加载器 | P0 | 3h |
| RAG 引擎：分块 + 嵌入 + 存储 | P0 | 4h |
| RAG 引擎：语义检索 | P0 | 3h |
| RAG 引擎：PDF/网页/代码加载器 | P1 | 6h |
| Prompt 引擎：CRUD + 版本管理 | P0 | 4h |
| Prompt 引擎：评分 + 优化建议 | P1 | 4h |
| 统一搜索接口 | P0 | 3h |
| CLI 入口（Typer + Rich） | P0 | 5h |
| 记忆引擎：对话摘要 | P1 | 3h |
| 数据导出功能 | P1 | 2h |
| 基础测试 | P1 | 4h |
| **合计** | | **~76h** |

### Phase 2：Web 可视化（2-3 周）

| 任务 | 优先级 |
|---|---|
| FastAPI 应用 & 路由 | P0 |
| Vue 3 项目搭建 | P0 |
| 仪表盘（知识概览、统计图表） | P0 |
| 记忆浏览与搜索页面 | P0 |
| 知识库管理页面（文档上传、列表） | P0 |
| Prompt 管理页面（编辑器、版本对比） | P0 |
| 知识图谱可视化（记忆关联关系） | P1 |
| 数据导入/导出 UI | P1 |

### Phase 3：增强功能（2 周）

| 任务 | 优先级 |
|---|---|
| 自动整理归类（定期 LLM 整理记忆，合并重复、补充关联） | P1 |
| 记忆衰减与权重调整（长期未访问的记忆降权） | P1 |
| 知识图谱（记忆之间的关联关系可视化） | P1 |
| 多用户支持（数据隔离） | P2 |
| API Key 认证 | P2 |

### Phase 4：开源发布（1 周）

| 任务 | 优先级 |
|---|---|
| README.md | P0 |
| pip install 支持 | P0 |
| Claude Code MCP 集成文档 | P0 |
| GitHub Actions CI/CD | P1 |
| 贡献指南 | P1 |

---

## 八、验证方案

```bash
# Phase 1: CLI
cd memora && pip install -e .
memora
> /ingest ./README.md
> /search 项目介绍
> /memory add "这是一个测试记忆" --type fact
> /memory list
> /prompt add "test" --content "你好{{name}}"
> /prompt score test 1 4.5

# Phase 1: MCP 自动记忆提取（核心验证）
# 1. 配置 .claude/settings.json 添加 memora MCP server
# 2. 在 Claude Code 中进行一次技术选型对话
# 3. 对话结束后检查 memora/memory list，确认记忆被自动提取
# 4. 开启新对话，问相关问题，确认 Claude 自动召回了之前的记忆
# 5. 验证去重：重复相同信息，确认不会产生重复记忆

# Phase 2: Web
uvicorn memora.api.app:app --reload
cd web && npm run dev

# 测试
pytest tests/ -v
```

---

## 九、与竞品的差异化

| 特性 | Memora | Mem.ai | Notion AI | Obsidian Copilot |
|---|---|---|---|---|
| 开源 | ✅ | ❌ | ❌ | ✅ |
| 本地优先 | ✅ | ❌ | ❌ | ✅ |
| **MCP 自动记忆提取** | **✅** | ❌ | ❌ | ❌ |
| 对话记忆提取 | ✅ | 部分 | ❌ | ❌ |
| Prompt 管理 | ✅ | ❌ | ❌ | ❌ |
| MCP 集成 | ✅ | ❌ | ❌ | ❌ |
| 多源 RAG | ✅ | ✅ | ✅ | 部分 |
| 数据可导出 | ✅ | 部分 | 部分 | ✅ |

> **MCP 自动记忆提取**是 Memora 的核心差异化：用户在 Claude Code 等工具中正常对话，知识自动积累，零成本录入。这是其他竞品都不具备的能力。
